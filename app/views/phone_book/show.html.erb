<% provide :page_title, "Massanutten Lions Phone Book" %>
<div class="page-header">
  <h1>McGaheysville-Massanutten Lions Club Phone Book</h1>
</div>
<h3>Total number of members: <%= @users.count %></h3>


<table class="table table-hover table-bordered table-striped table-condensed">
  <thead>
  <tr>
    <th><%= link_to "Last Name", :sort => "last_name" %>  <%= link_to "First Name", :sort => "first_name" %></th>
    <th><%= link_to "Email", :sort => "email" %></th>
    <th><%= link_to "Phone", :sort => "phone" %></th>
    <th><%= link_to "Avatar", :sort => "avatar" %></th>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
      <tr>
        <td><%= user.last_name+' '+user.first_name %></td>
        <td><%= user.email %></td>
        <td><%= user.phone %></td>
        <td><%= image_tag user.avatar.url(:thumb), :size => '40x40' %></td>

      </tr>
  <% end %>
  </tbody>
</table>