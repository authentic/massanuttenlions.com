<% provide :page_title, "Leadership" %>
<div class="page-header">
  <h1>Leadership <%= @current_year ? @current_year.current_year : "" %></h1>
</div>
<% @users.each do |user| %>
    <table align="left" class="table table-bordered table-condensed img-left">
      <tbody>
      <tr>
        <td>
          <%= image_tag user.avatar.url(:medium) %></td>
      </tr>
      <tr>
        <td align="center">
          <h6><%= user.leadership.upcase %> <br><%= user.signature %></h6>
        </td>
      </tr>
      </tbody>
    </table> <p><%= user.biography %></p>
    <div class="clearfix"></div>
<% end %>